set(TEST_DIR ${TEST_BASE_DIR}/self-test)
file(MAKE_DIRECTORY ${TEST_DIR})
set(TEST_SOURCES
  ${CMAKE_CURRENT_SOURCE_DIR}/configurations
  ${CMAKE_CURRENT_SOURCE_DIR}/mlips
  )
file(COPY ${TEST_SOURCES} DESTINATION ${TEST_DIR})

set(TEST_DEV_SOURCES ${PROJECT_SOURCE_DIR})

add_test(
  NAME UNIT_TEST
  COMMAND mlp self-test
  WORKING_DIRECTORY ${TEST_DIR}
)

add_test(
  NAME UNIT_TEST_DEV
  COMMAND mlp self-test-dev
  WORKING_DIRECTORY ${TEST_DIR}
)